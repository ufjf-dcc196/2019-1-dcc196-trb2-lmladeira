<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="br.ufjf.a2019_1dcc196trb2lucasmargato">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".TarefasTagActivity"></activity>
        <activity android:name=".TagsTarefaActivity" />
        <activity android:name=".DetalhesTarefaActivity" />
        <activity android:name=".NovaTagActivity" />
        <activity android:name=".GerenciarTagsActivity" />
        <activity android:name=".NovaTarefaActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>